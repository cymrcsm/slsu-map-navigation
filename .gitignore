# Node dependencies
node_modules/

# Database files (if generated dynamically by npm run db:init)
*.sqlite
*.db
*.fig

# Local TLS certs for testing live GPS on a phone (never commit private keys)
certs/

# OS files
.DS_Store
Thumbs.db

# thesis proposal PDF — kept locally, not committed (decide later)
docs/*.pdf
